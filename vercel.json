{
  "buildCommand": "ng build --configuration=production",
  "outputDirectory": "dist/emprescore_frontend/browser",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}